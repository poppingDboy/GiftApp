name: SwiftUI CI 

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Decode and Import Certificate
        run: |
          # Decode the base64-encoded certificate
          echo "${{ secrets.CERTIFICATE_BASE64 }}" | base64 --decode > certificate.p12

          # Decode the base64-encoded provisioning profile
          echo "${{ secrets.PROVISIONING_PROFILE_BASE64 }}" | base64 --decode > provisioning_profile.mobileprovision

          # Create and unlock a keychain
          security create-keychain -p "${{ secrets.CERTIFICATE_PASSWORD }}" build.keychain
          security unlock-keychain -p "${{ secrets.CERTIFICATE_PASSWORD }}" build.keychain

          # Import the certificate into the keychain
          security import certificate.p12 -k build.keychain -P "${{ secrets.CERTIFICATE_PASSWORD }}" -A

          # Import the provisioning profile
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp provisioning_profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/

          # Set the keychain as the default
          security list-keychains -d user -s build.keychain
          security default-keychain -s build.keychain

      - name: Build and test
        run: |
          xcodebuild -scheme Gift -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 14,OS=16.4' clean test -allowProvisioningUpdates
